function runFn() {
    let cmd = $("select.function > value:selected")

}



$(document).ready(function() {
    $("select.function").change(function() {
        let cmd = $("select.function > option:selected").val()
        
    }
}